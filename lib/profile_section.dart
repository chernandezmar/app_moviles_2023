import 'package:flutter/material.dart';
import 'profile_info.dart';


class muestra_profile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return MaterialApp(
        home: Scaffold(
        body: profile_info()
    )
    );
  }

}